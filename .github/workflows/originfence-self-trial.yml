name: OriginFence Self Trial

on:
  pull_request:
  merge_group:
    types:
      - checks_requested

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  originfence:
    name: originfence
    runs-on: ubuntu-latest
    env:
      ORIGINFENCE_CACHE_DIR: .originfence/cache

    steps:
      - name: Checkout merge candidate
        uses: actions/checkout@v6
        with:
          fetch-depth: 2

      - name: Restore OriginFence cache
        uses: actions/cache/restore@v5
        with:
          path: ${{ env.ORIGINFENCE_CACHE_DIR }}
          key: ${{ runner.os }}-originfence-self-${{ github.repository }}-${{ github.ref_name || github.ref }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-originfence-self-${{ github.repository }}-${{ github.ref_name || github.ref }}-
            ${{ runner.os }}-originfence-self-${{ github.repository }}-

      - name: Run OriginFence
        id: originfence
        uses: ./
        with:
          write-job-summary: "true"
          enforcement-mode: observe
          pr-comment: "true"
          comment-mode: review_and_block
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload OriginFence report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: originfence-report-${{ github.run_id }}-${{ github.job }}
          path: |
            ${{ steps.originfence.outputs.report-path }}
            ${{ steps.originfence.outputs.summary-path }}

      - name: Save OriginFence cache
        if: always()
        uses: actions/cache/save@v5
        with:
          path: ${{ env.ORIGINFENCE_CACHE_DIR }}
          key: ${{ runner.os }}-originfence-self-${{ github.repository }}-${{ github.ref_name || github.ref }}-${{ github.sha }}
